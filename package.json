{
  "name": "ai-perusteet-tests",
  "private": true,
  "scripts": {
    "verify:content": "python3 tests/test_content.py && python3 tests/test_yleiso.py && python3 tests/test_links.py && python3 tests/test_a11y.py",
    "verify:js": "node tests/test_generated_js.mjs",
    "verify:browser": "playwright test",
    "verify": "npm run verify:content && npm run verify:js",
    "verify:all": "npm run verify && npm run verify:browser"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.0"
  }
}
